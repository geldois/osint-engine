#!/bin/sh
command -v uv >/dev/null 2>&1 || {
    echo "osint-engine: 'uv' not found on PATH — activate mise (see README: Setup), or bypass once with 'git commit --no-verify'." >&2
    exit 1
}
exec uv run python -m scripts precommit
